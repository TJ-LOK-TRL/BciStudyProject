pips usados:
    pip install mne
    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
    pip install -e .
    pip install scikit-learn
    pip install transformers accelerate
    pip install pyriemann
    pip install dacite
    pip install moabb

comandos para todos terem acesso de leitura e "entrar no ficheiro" dos datasets para sempre evitando atualizar sempre que alguem baixa:
    sudo setfacl -R -m g:bci_eeg_team:rX /data/bci_eeg_data/
    sudo setfacl -R -d -m g:bci_eeg_team:rX /data/bci_eeg_data/

comandos usado:
    # Crie a estrutura profissional
    sudo groupadd bci_eeg_team
    sudo usermod -a -G bci_eeg_team tyrese
    sudo usermod -a -G bci_eeg_team salvador

    sudo mkdir -p /opt/bci_eeg_conda
    sudo chgrp -R bci_eeg_team /opt/bci_eeg_conda
    sudo chmod -R 770 /opt/bci_eeg_conda

    sudo find /opt/bci_eeg_conda -type d -exec chmod g+s {} +    
    #sudo chmod g+s /opt/bci_eeg_conda
